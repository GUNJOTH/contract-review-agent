# 变更日志

本文件记录每个发布版本面向使用者的可观察变更。

## [0.1.0] - 2026-09-20

### Added（新增）

- 发布首个合同审查应用版本，提供合同审查控制台、审查/预览/对比 API、异步任务 API，以及通过 OCR 网关完成扫描页和印章识别。
- 建立证据优先的模块化审查链路，覆盖规则快照与 Playbook、条款和事实抽取、候选检索、证据评估、风险分析、人工决策和修订集。
- 支持正式 RuleBundle、规则编辑、合同要素目录维护、AI 候选确认、合同版本比较和审查结果回放/审计。
- 提供 Redis/Celery 任务幂等、阶段事件账本、fencing token，以及模型重试、熔断和并发控制。
- 建立离线评测、Issue 模板、CI/CodeQL、版本标签与 GitHub Release 自动校验流程。

### Changed（变更）

- 审查结果统一为 `ReviewResult` schema 2.0；规则适用性和审查结论统一绑定证据评估，证据不足时保留 `UNKNOWN`。
- OCR、embedding、模型和 Redis 均通过可替换边界接入，普通测试默认关闭外部依赖；CI 必需 Python 版本收敛为 3.12。

### Fixed（修复）

- 修复规则编辑、AI 候选确认、合同版本比较、异步任务回放及并发审查流程中的一致性问题。
- 修复任务幂等登记、租约 fencing、模型传输重试和熔断边界，避免旧 worker 覆盖新任务状态。
- 修复 CI pytest 临时目录、依赖锁定校验和发布元数据校验流程。

### Security（安全）

- 受保护 API 在未配置或未提供有效 Token 时 fail-closed；外部模型调用增加 PII 门禁。
- 对外错误响应改为通用错误信息；硬件健康接口不再回传 GPU 异常原文，降低堆栈和内部实现信息泄露风险。

## [Unreleased]

<!-- 发布前按 Added（新增）、Changed（变更）、Fixed（修复）和 Security（安全）分类补充条目。 -->
