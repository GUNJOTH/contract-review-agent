# 合同审查项目规则

本仓库是证据优先的合同审查模块化单体。项目事实以源码、规则快照、测试、评测夹具和架构文档为准；全局编码技能只提供通用工程约束。

## 修改范围

- 合同审查智能体默认修改后端代码、规则和后端接口；除非用户明确要求，不修改前端。
- 新增或修改的代码注释、文档字符串、TODO 和面向开发者的说明使用简体中文；协议字段名、枚举值、类名和函数名按现有接口保持兼容。
- 合同审查的企业立场、优选/备选/禁止文本、缺失条款策略、建议动作和升级条件必须写入版本化规则定义（Playbook），不得只硬编码在执行器或提示词中。
- Playbook 判断必须绑定合同条款和规则来源证据；无法从证据确定的内容输出 `UNKNOWN` 或人工升级，不得默认通过。

## 验证与边界

- 编码阶段负责后端功能实现和静态边界检查；功能实现后进入独立测试阶段，测试不得通过修改生产代码掩盖失败。
- 固定离线评测集不得访问 OCR、Redis、Celery 或外部模型；真实 Redis、OCR、模型和 Docker 联调按 README 的独立验收命令执行。
- 不把离线夹具、模拟服务或局部 API smoke 结果写成真实外部系统验收。
- 不把真实合同、个人信息、密钥或外部服务凭证提交到仓库、评测夹具或日志。

## 入口

- 运行和环境边界：`README.md`；
- 架构和长期决策：`docs/ARCHITECTURE.md`、`docs/decisions/`；
- 固定离线评测：`evals/README.md` 和 `evals/contract_review_cases.json`；
- 具体规则来源：`data/contract_rules_v*.json` 和 Playbook 实现。
